1. **Purpose** – New concept: execute the console quest with `dotnet run`.
2. **Recipe List**

| Concept | Syntax / Example | Docs |
| --- | --- | --- |
| Run console app | `dotnet run` | [Run console app](https://learn.microsoft.com/dotnet/core/tools/dotnet-run) |

3. **Adventure Story / Problem** – The kiosk boots but demands proof that John can launch spells on demand. A snarky swamp frog insists that only code triggered with `dotnet run` counts toward swamp entry. Inside the project, print two status lines that make it obvious the chant came from an executed app. Run it with `dotnet run` to appease the frog.

4. **Example Input/Output**

```
Output
Executing labyrinth link...
Router spirit answered!
```
