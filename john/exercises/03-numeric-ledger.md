1. **Purpose** – New concept: declare numeric primitives (`int` and `double`).
2. **Recipe List**

| Concept | Syntax / Example | Docs |
| --- | --- | --- |
| Declare numeric variables | `int corridorTurns = 12;` | [Declare numeric variables](https://learn.microsoft.com/dotnet/csharp/language-reference/builtin-types/numeric-types) |

3. **Adventure Story / Problem** – Before entering the maze, John scribbles a corridor ledger. The kiosk only waves him through if the log clearly separates whole-number turns from fractional torch power. Create an `int` variable for turns and a `double` for torch power. Print each value on its own line so the ledger is neat.

4. **Example Input/Output**

```
Output
12
3.5
```
