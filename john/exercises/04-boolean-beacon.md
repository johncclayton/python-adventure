1. **Purpose** – New concept: store quest state in a `bool`.
2. **Recipe List**

| Concept | Syntax / Example | Docs |
| --- | --- | --- |
| Boolean variable | `bool beaconLit = true;` | [Boolean variable](https://learn.microsoft.com/dotnet/csharp/language-reference/builtin-types/bool) |

3. **Adventure Story / Problem** – A labyrinth gargoyle refuses to report signal strength unless John tracks whether the Wi-Fi torch is lit. The statue loves `true`/`false` more than riddles. Create a `bool` indicating whether the signal torch is active and print a friendly message that includes the value.

4. **Example Input/Output**

```
Output
Signal torch lit? True
```
