1. **Purpose** – New concept: compare values with relational operators to produce booleans.
2. **Recipe List**

| Concept | Syntax / Example | Docs |
| --- | --- | --- |
| Comparison operators | `bool isCalm = slimeLevel < 5;` | [Comparison operators](https://learn.microsoft.com/dotnet/csharp/language-reference/operators/comparison-operators) |

3. **Adventure Story / Problem** – The swamp issues a comparison compass that glows only when John checks if slime is below danger levels. Store a slime level and a safety threshold. Compare them with `>` or `<`, assign the result to a `bool`, and print the answer.

4. **Example Input/Output**

```
Output
Is slime manageable? True
```
