1. **Purpose** – New concept: branch with `if`/`else`.
2. **Recipe List**

| Concept | Syntax / Example | Docs |
| --- | --- | --- |
| if/else statement | `if (noiseLevel > 5) { ... } else { ... }` | [if/else statement](https://learn.microsoft.com/dotnet/csharp/language-reference/statements/selection-statements#the-if-statement) |

3. **Adventure Story / Problem** – A rickety bridge listens for polite decision making. John must whisper whether to tiptoe or stomp based on the swamp's sarcasm level. Store a sarcasm score. If it's above 5, print a warning; otherwise print that the path is clear.

4. **Example Input/Output**

```
Output
Bridge protocol: tiptoe softly.
```
