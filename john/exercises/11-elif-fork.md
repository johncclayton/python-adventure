1. **Purpose** – New concept: extend decisions with `else if` chains.
2. **Recipe List**

| Concept | Syntax / Example | Docs |
| --- | --- | --- |
| else-if chain | `if (...) { } else if (...) { } else { }` | [else-if chain](https://learn.microsoft.com/dotnet/csharp/language-reference/statements/selection-statements#the-if-statement) |

3. **Adventure Story / Problem** – Three singing frogs share a fork in the swamp. Each croaks a different tune depending on the humidity tier John reports. Read a humidity level variable and use an `if/else if/else` chain to print one of three travel tips.

4. **Example Input/Output**

```
Output
Tip: deploy umbrella fins.
```
