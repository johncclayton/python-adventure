1. **Purpose** – New concept: declare and call a `static` method.
2. **Recipe List**

| Concept | Syntax / Example | Docs |
| --- | --- | --- |
| Static method | `QuestPlanner.BuildCheer(name);` | [Static method](https://learn.microsoft.com/dotnet/csharp/programming-guide/classes-and-structs/static-classes-and-static-class-members) |

3. **Adventure Story / Problem** – A brass automaton salutes only when a proper static fanfare is invoked. Write a `static` method that accepts a biome name and returns a cheer string. Call it from `Main` (or top level) and print the result.

4. **Example Input/Output**

```
Output
Fanfare for Labyrinth launched!
```
