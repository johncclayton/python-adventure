1. **Purpose** – New concept: model light data with a `struct`.
2. **Recipe List**

| Concept | Syntax / Example | Docs |
| --- | --- | --- |
| Define struct | `struct RunePoint { public int Row; }` | [Define struct](https://learn.microsoft.com/dotnet/csharp/language-reference/builtin-types/struct) |

3. **Adventure Story / Problem** – A survey sprite insists the map grid is drawn with structs so coordinates stay lean and snappy. Declare a `struct` representing a rune's row and column. Instantiate it and print both values.

4. **Example Input/Output**

```
Output
Rune row: 2
Rune column: 5
```
