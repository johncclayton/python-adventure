1. **Purpose** – New concept: grow and trim collections with `List<T>`.
2. **Recipe List**

| Concept | Syntax / Example | Docs |
| --- | --- | --- |
| List operations | `var supplies = new List<string>();` | [List operations](https://learn.microsoft.com/dotnet/api/system.collections.generic.list-1) |

3. **Adventure Story / Problem** – The swamp snack bar only accepts orders tracked in a flexible list—otherwise the chef pelican refuses to cook. Start a `List<string>` with one supply, add another, remove the first, then loop through the remaining items and print them.

4. **Example Input/Output**

```
Output
Still packed: extra socks
```
