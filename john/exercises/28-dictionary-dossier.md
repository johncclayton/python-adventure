1. **Purpose** – New concept: map keys to values with `Dictionary<TKey, TValue>`.
2. **Recipe List**

| Concept | Syntax / Example | Docs |
| --- | --- | --- |
| Dictionary lookup | `var map = new Dictionary<string, int>();` | [Dictionary lookup](https://learn.microsoft.com/dotnet/api/system.collections.generic.dictionary-2) |

3. **Adventure Story / Problem** – Checkpoint mimics only unlock when John proves he can pair each biome with its guard count. Create a `Dictionary<string, int>` with two biome entries. Print a formatted message that includes one biome's guard count using the dictionary lookup.

4. **Example Input/Output**

```
Output
Labyrinth employs 4 guards.
```
