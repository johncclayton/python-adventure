1. **Purpose** – New concept: use built-in `Action` and `Func` with lambdas.
2. **Recipe List**

| Concept | Syntax / Example | Docs |
| --- | --- | --- |
| Action & Func | `Action wave = () => ...;` | [Action & Func](https://learn.microsoft.com/dotnet/api/system.action) |

3. **Adventure Story / Problem** – Crystal spotlights only track heroes who choreograph lambdas for both actions and calculations. Create an `Action` that prints a wave and a `Func<int, int>` that doubles sparkles. Invoke both and print their effects.

4. **Example Input/Output**

```
Output
Waving to the crowd!
Doubled sparkles: 8
```
