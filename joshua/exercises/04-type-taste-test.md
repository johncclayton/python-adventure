1. **Purpose** – Help Joshua inspect data types before he dumps stats into the mystery blender.
2. **Recipe List**

| Concept | Syntax / Example | Docs |
| --- | --- | --- |
| Inspect a value's type | `type(points)` | [Inspect a value's type](https://docs.python.org/3/library/functions.html#type) |

3. **Adventure Story / Problem** – The Swolestice Spin Class Coliseum runs on picky machinery. A holo-chicken refuses to reveal the next hint until Joshua proves he knows which stats are ints, strings, and bools. Store Joshua's points (int), favorite shake flavor (string), and stretch status (bool). Print each value alongside the result of calling `type()` on it.

4. **Example Input/Output**

```
Output
Points: 42 -> <class 'int'>
Flavor: Peanut Butter Blast -> <class 'str'>
Stretched: True -> <class 'bool'>
```
