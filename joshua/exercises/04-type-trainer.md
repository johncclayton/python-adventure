1. **Purpose** – New concept: inspect values with `type()`.
2. **Recipe List**

| Concept | Syntax / Example | Docs |
| --- | --- | --- |
| Use type() | `type(points)` | [Use type()](https://docs.python.org/3/library/functions.html#type) |

3. **Adventure Story / Problem** – A protein shaker robot asks Joshua to confirm each stat's type before it mixes the post-game smoothie. Store a score (int), a favorite shake flavor (string), and the warmup status (bool). Print each value and the result of `type()` for it.

4. **Example Input/Output**

```
Output
Score: 18 -> <class 'int'>
Shake: Banana Blitz -> <class 'str'>
Warmup done: False -> <class 'bool'>
```
