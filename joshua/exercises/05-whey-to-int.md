1. **Purpose** – Convert string input into integers so Joshua can tally protein scoops correctly.
2. **Recipe List**

| Concept | Syntax / Example | Docs |
| --- | --- | --- |
| Convert text to integers | `scoops = int(input_text)` | [Convert text to integers](https://docs.python.org/3/library/functions.html#int) |

3. **Adventure Story / Problem** – A basket of whey cupcakes hides the coordinates of the next drill. The guarding rooster agrees to share only if Joshua can convert crowd-reported scoop counts into actual numbers. Ask the user how many protein scoops they added to the blender. Cast the result to an int, then print the total scoops doubled to predict tomorrow's order.

4. **Example Input/Output**

```
Input
4
Output
Tomorrow requires 8 scoops.
```
