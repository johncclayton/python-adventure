1. **Purpose** – New concept: evaluate truthiness with `bool()`.
2. **Recipe List**

| Concept | Syntax / Example | Docs |
| --- | --- | --- |
| bool() on values | `bool(value)` | [bool() on values](https://docs.python.org/3/library/functions.html#bool) |

3. **Adventure Story / Problem** – Coach installed a truth bench that tips over if Joshua can't predict which stats count as truthy reps. Create an empty string, a list with one dumbbell weight, and zero misses. Print each value and its `bool()` result.

4. **Example Input/Output**

```
Output
Locker note -> False
Weight list -> True
Misses -> False
```
