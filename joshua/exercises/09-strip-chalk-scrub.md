1. **Purpose** – Use `.strip()` to clean crowd input before the protein chalkboard records it.
2. **Recipe List**

| Concept | Syntax / Example | Docs |
| --- | --- | --- |
| Remove leading and trailing whitespace | `clean = raw.strip()` | [Remove leading and trailing whitespace](https://docs.python.org/3/library/stdtypes.html#str.strip) |

3. **Adventure Story / Problem** – The arena chalkboard is coated in protein powder dust. A prankster chicken entered the next clue with messy spaces, so Joshua has to tidy it before the board accepts it. Start with a string that has leading and trailing spaces plus a newline. Use `.strip()` to clean it, then print the cleaned result inside angle brackets so everyone sees it's tidy.

4. **Example Input/Output**

```
Output
<Broccoli Dunk Bunker>
```
