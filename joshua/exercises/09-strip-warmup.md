1. **Purpose** – New concept: tidy text with `.strip()`.
2. **Recipe List**

| Concept | Syntax / Example | Docs |
| --- | --- | --- |
| str.strip() | `chant.strip()` | [str.strip()](https://docs.python.org/3/library/stdtypes.html#str.strip) |

3. **Adventure Story / Problem** – The warmup playlist refuses to load if there are extra spaces before or after the chant name. Store a chant string with messy spaces on both sides. Use `.strip()` to clean it and print the trimmed chant.


my_input = input("Type something: ")
        

4. **Example Input/Output**

```
Output
Clean chant: Kale Crushers
```
