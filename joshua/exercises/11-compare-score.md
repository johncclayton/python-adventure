1. **Purpose** – New concept: compare values with `==` and `!=`.
2. **Recipe List**

| Concept | Syntax / Example | Docs |
| --- | --- | --- |
| Comparison operators | `shots == goal` | [Comparison operators](https://docs.python.org/3/library/stdtypes.html#comparisons) |

3. **Adventure Story / Problem** – The shot-clock goblin checks whether practice goals were hit or missed before opening the smoothie bar. Store the target shots and actual shots. Print whether Joshua met the goal and whether the score differed using `==` and `!=`.

4. **Example Input/Output**

```
Output
Goal met? True
Different than target? False
```
